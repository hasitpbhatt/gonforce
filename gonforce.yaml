package: github.com/hasitpbhatt/gonforce

default:
  type: blacklist
  imports:
    - gopkg.in
  except:
    - gopkg.in/yaml.v2

rules:
  - name: models
    rule:
      type: blacklist
      imports:
        - github.com/hasitpbhatt/gonforce

