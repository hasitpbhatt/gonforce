package: github.com/hasitpbhatt/gonforce

default:
  type: blocklist
  imports:
    - gopkg.in
  except:
    - gopkg.in/yaml.v2

rules:
  - name: models
    rule:
      type: blocklist
      imports:
        - github.com/hasitpbhatt/gonforce

