package: github.com/hasitpbhatt/gonforce

default:
  type: blocklist
  imports:
    - gopkg.in
  except:
    - gopkg.in/yaml.v2

rules:
  - name: models/random
    rule:
      type: allowlist
      imports:
        - path/filepath