package: github.com/hasitpbhatt/gonforce

default:
  type: blacklist
  imports:
    - gopkg.in
  except:
    - gopkg.in/yaml.v2

rules:
  - name: models/random
    rule:
      type: whitelist
      imports:
        - path/filepath